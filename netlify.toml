[build]
  command = "pnpm run build"
  publish = "dist"

[build.environment]
  PNPM_VERSION = "10"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/reservas"
  to = "/.netlify/functions/reservas"
  status = 200
  force = true
